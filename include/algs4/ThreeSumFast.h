#ifndef TREESUMFAST_H
#define TREESUMFAST_H

#include <vector>

namespace algs4 {
namespace ThreeSumFast {

bool containsDuplicates(std::vector<int> &a);
void printAll(std::vector<int> &a);
int count(std::vector<int> &a);

}
}

#endif
